<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TestLLM.DropDown"
             x:Name="ThisDropDown">
    <VerticalStackLayout Spacing="0">
        <!-- Custom Dropdown -->
        <Border x:Name="DropdownFrame"
                BackgroundColor="White"
                Stroke="#E2E8F0"
                StrokeThickness="1"
                Padding="12,8"
                Margin="0,4,0,0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="8" />
            </Border.StrokeShape>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Label x:Name="SelectedValueLabel"
                       Text="1000"
                       VerticalOptions="Center"
                       FontSize="14"
                       TextColor="#1E293B"
                       Grid.Column="0" />
                <Label Text="▼"
                       VerticalOptions="Center"
                       FontSize="12"
                       TextColor="#64748B"
                       Grid.Column="1"
                       Margin="8,0,0,0" />
                <Button x:Name="DropdownButton"
                        BackgroundColor="Transparent"
                        Grid.ColumnSpan="2"
                        Clicked="OnDropdownClicked" />
            </Grid>
        </Border>
        <!-- Dropdown Options -->
        <Border x:Name="DropdownOptionsFrame"
                BackgroundColor="White"
                Stroke="#E2E8F0"
                StrokeThickness="1"
                Padding="0"
                Margin="0,2,0,0"
                IsVisible="False">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="8" />
            </Border.StrokeShape>
            <CollectionView x:Name="DropdownOptions"
                           ItemsSource="{Binding ItemsSource, Source={x:Reference Name=ThisDropDown}}"
                           SelectionMode="Single"
                           SelectionChanged="OnDropdownOptionSelected">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border BackgroundColor="Transparent"
                                Padding="12,8"
                                Margin="0,1">
                            <Label Text="{Binding}"
                                   FontSize="14"
                                   TextColor="#1E293B"
                                   VerticalOptions="Center" />
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Border>
    </VerticalStackLayout>
</ContentView> 